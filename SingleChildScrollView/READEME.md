# Flutter ScrollView 종합 정리

## 1. ListView vs SingleChildScrollView vs CustomScrollView

### 핵심 차이: 렌더링 방식

```dart
// ListView - 화면에 보이는 것만 빌드 (Lazy Loading)
ListView.builder(
itemCount: 100,
itemBuilder: (context, index) => ListTile(title: Text('$index')),
)

// SingleChildScrollView - 전부 한번에 빌드 (Eager Loading)
SingleChildScrollView(
child: Column(
children: List.generate(100, (i) => ListTile(title: Text('$i'))),
),
)
```

| 구분 | ListView | SingleChildScrollView | CustomScrollView |
|------|----------|----------------------|------------------|
| **렌더링** | Lazy (보이는 것만) | Eager (전부) | Lazy |
| **성능** | 많은 아이템에 좋음 | 적은 아이템에 적합 | 좋음 |
| **유연성** | 단순 리스트 | 단순 스크롤 | 복잡한 조합 가능 |
| **하단 고정** | 어려움 | ConstrainedBox 필요 | SliverFillRemaining |

### 선택 가이드

```
아이템 많음 (10개+)           → ListView.builder
아이템 적음 + 스크롤 필요      → SingleChildScrollView
복잡한 스크롤 효과 필요        → CustomScrollView
하단 고정 + 스크롤             → CustomScrollView + SliverFillRemaining
                              또는 Column + Expanded
```

---

## 2. Tight Constraints vs Loose Constraints

### 개념

```dart
// Tight Constraints: 자식에게 정확한 크기를 강제
minHeight = maxHeight = 500  // "무조건 500이야!"
minWidth = maxWidth = 300

// Loose Constraints: 자식에게 범위를 줌
minHeight = 0, maxHeight = 500  // "0~500 사이에서 네가 정해"
minWidth = 0, maxWidth = 300
```

### SliverFillRemaining의 동작

```dart
SliverFillRemaining(
  hasScrollBody: false,
  child: Container(
    height: 100,  // ❌ 무시됨! (tight constraints)
    color: Colors.yellow,
  ),
)
```

`SliverFillRemaining`은 **tight constraints**를 줘서 자식의 height/width를 무시합니다.

### 해결책: alignment로 loose constraints 만들기

```dart
SliverFillRemaining(
  hasScrollBody: false,
  child: Container(
    alignment: Alignment.bottomCenter,  // ← Align 위젯 내부 생성
    child: Container(
      height: 100,  // ✅ 동작함!
      color: Colors.yellow,
    ),
  ),
)
```

**왜 되는가?**

```dart
// Container 내부 동작 (단순화)
if (alignment != null) {
  child = Align(           // Align 위젯이 생성됨
    alignment: alignment,
    child: child,          // 이 child에게 loose constraints 전달
  );
}
```

`Align`, `Center`, `Stack` 같은 위젯들이 **tight → loose로 변환**해줍니다.

---

## 3. 하단 고정 방법 비교

### 방법 1: ConstrainedBox 방식 (옛날 방식)

```dart
SingleChildScrollView(
  child: ConstrainedBox(
    constraints: BoxConstraints(
      minHeight: MediaQuery.of(context).size.height
                 - MediaQuery.of(context).padding.top
                 - kToolbarHeight
                 - MediaQuery.of(context).padding.bottom,  // 😵 복잡한 계산
    ),
    child: Column(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      children: [
        /* 상단 콘텐츠 */,
        /* 하단 고정 */,
      ],
    ),
  ),
)
```

### 방법 2: CustomScrollView + SliverFillRemaining

```dart
CustomScrollView(
  slivers: [
    SliverToBoxAdapter(child: /* 상단 콘텐츠 */),
    SliverFillRemaining(
      hasScrollBody: false,
      child: Align(
        alignment: Alignment.bottomCenter,
        child: /* 하단 고정 */,
      ),
    ),
  ],
)
```

### 방법 3: Column + Expanded (권장)

```dart
Column(
  children: [
    Expanded(
      child: SingleChildScrollView(
        child: /* 스크롤 콘텐츠 */,
      ),
    ),
    Container(height: 100, color: Colors.yellow),  // 하단 고정
  ],
)
```

### 비교

| 구분 | ConstrainedBox | CustomScrollView | Column + Expanded |
|------|----------------|------------------|-------------------|
| **코드량** | 많음 | 중간 | 적음 ✅ |
| **MediaQuery 계산** | 필요 😵 | 불필요 | 불필요 ✅ |
| **가독성** | 복잡 | 중간 | 직관적 ✅ |
| **하단 위치** | 스크롤 안 | 스크롤 안 | 스크롤 밖 |

---

## 4. 키보드와 resizeToAvoidBottomInset

### Scaffold의 동작

```dart
Scaffold(
  resizeToAvoidBottomInset: true,  // 기본값
  // true: 키보드 올라오면 body 높이가 줄어듦
  // false: body 높이 유지, 키보드가 위를 덮음
)
```

---

### 핵심: "콘텐츠가 스크롤 안에 있냐 밖에 있냐"

#### SingleChildScrollView (하단이 스크롤 안에 있음)

```
키보드 올라오기 전:              키보드 올라온 후:
┌─────────────────────┐        ┌─────────────────────┐
│       AppBar        │        │       AppBar        │
├─────────────────────┤        ├─────────────────────┤
│  ┌───────────────┐  │        │  ┌───────────────┐  │
│  │ 폼 입력       │  │        │  │ 폼 입력       │  │
│  │               │  │   →    │  └───────────────┘  │ body가
│  │               │  │        ├─────────────────────┤ 여기까지만!
│  │ 하단 버튼     │  │        │      ⌨️ 키보드       │
│  └───────────────┘  │        │                     │
└─────────────────────┘        └─────────────────────┘
                                     [하단 버튼] 👻
                                     (스크롤해야 보임)
```

**왜?** 하단 버튼이 스크롤 "안"에 있어서, body가 줄어들면 **Viewport 밖으로 밀려남** → 스크롤해야 보임

```dart
// ❌ 하단 버튼이 스크롤 안에 → 키보드에 가려짐
Scaffold(
  body: SingleChildScrollView(
    child: Column(
      children: [
        TextFormField(),
        SizedBox(height: 500),
        ElevatedButton(child: Text('제출')),  // 스크롤 안!
      ],
    ),
  ),
)
```

---

#### Column + Expanded (하단이 스크롤 밖에 있음)

```
키보드 올라오기 전:              키보드 올라온 후:
┌─────────────────────┐        ┌─────────────────────┐
│       AppBar        │        │       AppBar        │
├─────────────────────┤        ├─────────────────────┤
│  ┌───────────────┐  │        │  ┌───────────────┐  │
│  │ 폼 입력       │  │        │  │ 폼 입력       │  │ Expanded
│  │               │  │   →    │  └───────────────┘  │ (줄어듦)
│  │               │  │        ├─────────────────────┤
│  └───────────────┘  │        │    [하단 버튼] ✅    │ 항상 보임!
├─────────────────────┤        ├─────────────────────┤
│    [하단 버튼]       │        │      ⌨️ 키보드       │
└─────────────────────┘        └─────────────────────┘
```

**왜?** 하단 버튼이 스크롤 "밖"에 있어서, body가 줄어들면 **Expanded만 줄어들고** 하단은 키보드 바로 위에 붙음

```dart
// ✅ 하단 버튼이 스크롤 밖에 → 항상 보임!
Scaffold(
  body: Column(
    children: [
      Expanded(
        child: SingleChildScrollView(
          child: Column(
            children: [
              TextFormField(),
              SizedBox(height: 500),
            ],
          ),
        ),
      ),
      ElevatedButton(child: Text('제출')),  // 스크롤 밖!
    ],
  ),
)
```

- Scaffold가 body 높이를 줄임
- Column이 줄어든 공간 안에서 재배치
- 하단 고정(100px)은 **고정**
- `Expanded`가 **희생**되어 줄어듦

---

### resizeToAvoidBottomInset: false로 하면?

```dart
Scaffold(
  resizeToAvoidBottomInset: false,  // body 크기 안 줄임
  body: SingleChildScrollView(...),
)
```

```
키보드 올라온 후 (false):
┌─────────────────────┐
│       AppBar        │
├─────────────────────┤
│  ┌───────────────┐  │
│  │ 폼 입력       │  │  body 크기
│  │               │  │  그대로 유지!
│  │               │  │
│  │ 하단 버튼     │← ┼──── 키보드가 덮어버림
│  └───────────────┘  │
├─────────────────────┤
│      ⌨️ 키보드       │
└─────────────────────┘
```

**결과:** body는 그대로인데 키보드가 위를 덮음 → **overflow 에러!** (스크롤 없으면)

---

### 왜 false면 스크롤해도 안 보일까?

**스크롤의 목적:** "Viewport 밖에 있는 콘텐츠"를 "Viewport 안으로" 끌어오는 것

```
true (Viewport 줄어듦):           false (Viewport 그대로):

┌─────────┐ Viewport              ┌─────────┐ Viewport
│ 콘텐츠1 │                       │ 콘텐츠1 │
│ 콘텐츠2 │                       │ 콘텐츠2 │
└─────────┘ ← Viewport 끝         │ 콘텐츠3 │ ← 이미 안에 있음
   ⌨️ 키보드                       │ [버튼]  │ ← 이미 안에 있음
- - - - - -                       └─────────┘ ← Viewport 끝
 [콘텐츠3]  ← 밖에 있음!              ⌨️ 키보드 (덮음)
 [버튼]     ← 밖에 있음!

→ 스크롤로 끌어올림 ✅             → 이미 다 안에 있어서 끌어올릴 게 없음
                                 → 근데 키보드가 가림 ❌
```

**한 줄 요약:** `false`면 콘텐츠가 이미 Viewport 안에 있어서 스크롤이 소용없고, 키보드만 가릴 뿐!

---

### 정리 표

| 설정 | body 크기 | 결과 |
|------|----------|------|
| `true` (기본) | 줄어듦 | 스크롤 가능, 하단은 스크롤해야 보임 |
| `false` | 유지 | 키보드가 덮음, overflow 위험 |

| 구조 | 하단 위치 | 키보드 올라올 때 |
|------|----------|-----------------|
| SingleChildScrollView 안 | 스크롤 안 | 스크롤해야 보임 👻 |
| Column + Expanded 밖 | 스크롤 밖 | 항상 보임 ✅ |

---

## 5. Viewport와 Clipping

### 스크롤 시 일어나는 일

```
Expanded 영역 (Viewport = 보이는 창)
┌─────────────────────┐
│ ┌─────────────────┐ │
│ │  콘텐츠 1       │ │ ← 보임
│ │                 │ │
│ │  콘텐츠 2       │ │ ← 보임
│ └───────┬─────────┘ │
├─────────┼───────────┤ ← 여기서 잘림 (clip)
│  하단 고정           │
└─────────────────────┘
          │
    ┌─────┴─────────┐
    │  콘텐츠 3     │ ← 존재하지만 안 보임 (clipped)
    │  콘텐츠 4     │ ← 존재하지만 안 보임
    └───────────────┘
```

### 스크롤 전 vs 후

```
스크롤 전:                    스크롤 후:
┌───────────────┐            ┌───────────────┐
│ [콘텐츠 1] ✓  │            │ [콘텐츠 2] ✓  │
│ [콘텐츠 2] ✓  │     →      │ [콘텐츠 3] ✓  │
├───────────────┤            ├───────────────┤
│ [하단 고정]   │            │ [하단 고정]   │
└───────────────┘            └───────────────┘
   [콘텐츠 3] 👻               [콘텐츠 1] 👻
   [콘텐츠 4] 👻               [콘텐츠 4] 👻
   (잘려서 안보임)              (잘려서 안보임)
```

**핵심:** 하단 고정 "뒤로" 가는 게 아니라, Viewport "밖으로" 잘려나가는 것!

### SingleChildScrollView vs ListView의 메모리 차이

```
SingleChildScrollView + Column:
 → 모든 콘텐츠가 메모리에 존재
 → 화면 밖은 "잘려서" 안 보일 뿐
 → 스크롤하면 다시 보임

ListView.builder:
 → 보이는 것만 메모리에 존재 (Lazy)
 → 안 보이는 건 아예 없음 (destroy)
 → 스크롤하면 새로 생성 (build)
```

---

## 6. Sliver 프로토콜

### 일반 위젯 vs Sliver 위젯

```
일반 위젯: BoxConstraints (width, height)
Sliver 위젯: SliverConstraints (viewport, scroll offset, ...)
```

### CustomScrollView에서 사용

```dart
CustomScrollView(
  slivers: [
    SliverAppBar(),           // ✅ Sliver 위젯
    SliverList(),             // ✅ Sliver 위젯
    SliverToBoxAdapter(       // 변환기: 일반 → Sliver
      child: Container(),     // ✅ 일반 위젯
    ),
    Container(),              // ❌ 에러! 일반 위젯 직접 못 넣음
  ],
)
```

---

## 7. 실전 선택 가이드

```
┌─────────────────────────────────────────────────────────────┐
│ 상황별 추천                                                  │
├─────────────────────────────────────────────────────────────┤
│ 단순 리스트 (아이템 많음)        → ListView.builder           │
│ 폼 + 키보드 대응                → SingleChildScrollView      │
│ 하단 버튼 항상 보이게           → Column + Expanded ✅        │
│ 하단 버튼 스크롤에 포함          → CustomScrollView           │
│ SliverAppBar + 복잡한 스크롤     → CustomScrollView           │
└─────────────────────────────────────────────────────────────┘
```

### 하단 고정 버튼 (로그인 폼 등)

```dart
// 추천: Column + Expanded
Scaffold(
  body: Column(
    children: [
      Expanded(
        child: SingleChildScrollView(
          child: Column(
            children: [
              TextFormField(...),
              TextFormField(...),
            ],
          ),
        ),
      ),
      ElevatedButton(  // 키보드 올라와도 항상 보임!
        onPressed: () {},
        child: Text('로그인'),
      ),
    ],
  ),
)
```
